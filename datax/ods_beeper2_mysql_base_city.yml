job:
  content:
  - reader:
      name: mysqlreader
      parameter:
        connection:
        - jdbcUrl:
          - 'jdbc:mysql://${mysql_ip}:${mysql_port}/${mysql_database}'
          table:
          - base_city
        username: '${mysql_username}'
        password: '${mysql_password}'
        where: ''
        column:
        - id
        - name
        - adc_id
        - province_id
        - traffic_limit
        - enable_driver
        - enable_warehouse
        - default_addr
        - default_addr_lot
        - default_addr_lat
    writer:
      name: odpswriter
      parameter:
        odpsServer: http://service.odps.aliyun.com/api
        accessId: '${odps_access_id}'
        accessKey: '${odps_access_key}'
        project: '${warehouse_project}'
        table: ods_beeper2_mysql_base_city_snapshots
        partition: 'snapshot_date=${snapshot_date}'
        truncate: true
        tunnelServer: ''
        column:
        - id
        - name
        - adc_id
        - province_id
        - traffic_limit
        - enable_driver
        - enable_warehouse
        - default_addr
        - default_addr_lot
        - default_addr_lat
  setting:
    speed:
      channel: '1'

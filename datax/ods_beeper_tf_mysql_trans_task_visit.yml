job:
  content:
  - reader:
      name: mysqlreader
      parameter:
        connection:
        - jdbcUrl:
          - 'jdbc:mysql://${mysql_ip}:${mysql_port}/${mysql_database}'
          table:
          - trans_task_visit
        username: '${mysql_username}'
        password: '${mysql_password}'
        where: ''
        column:
        - id
        - trans_task_id
        - driver_id
        - created_at
    writer:
      name: odpswriter
      parameter:
        odpsServer: http://service.odps.aliyun.com/api
        accessId: '${odps_access_id}'
        accessKey: '${odps_access_key}'
        project: '${warehouse_project}'
        table: ods_beeper_tf_mysql_trans_task_visit_snapshots
        partition: 'snapshot_date=${snapshot_date}'
        truncate: true
        tunnelServer: ''
        column:
        - id
        - trans_task_id
        - driver_id
        - created_at
  setting:
    speed:
      channel: '1'
